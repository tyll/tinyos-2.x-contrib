<template description="Set sensor input (Multiple)" category="tutorial"><line># Requires: PseudoSensor and mote reading from channel 0.</line><line># PseudoSensor support MUST be compiled in</line><line>from simx.sensor.util import sineWave</line><line>fn = sineWave(T, 20, 8000,</line><line>  phase=0, offset=8000)</line><line>Sensor.connect(0, 0, fn)</line><line></line><line>fn = sineWave(T, 30, 8000,</line><line>  phase=0, offset=8000)</line><line>Sensor.connect(1, 0, fn)</line><line></line><line>fn = sineWave(T, 40, 8000,</line><line>  phase=0, offset=8000)</line><line>Sensor.connect(2, 0, fn)</line></template>