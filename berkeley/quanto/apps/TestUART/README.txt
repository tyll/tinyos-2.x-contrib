TestUART waits for the button to be pressed then sends 3 packets
over the UART, of increasing size: 8, 16, and 28 bytes.
It then sends the Context log.

The goal is just to see what happens when using the UART.

