<template description="Accessing Motes" category="tutorial"><line># Requires: TossimTopo</line><line></line><line># TossimTopo adds some conveniece extensions to the standard</line><line># Tossim object. When the default harness is used, the</line><line># global variables T can be used to access TossimTopo.</line><line></line><line># This is just a helper to turn a List of nodes into IDs.</line><line>def getIDs(nodes):</line><line>  return [x.id() for x in nodes]</line><line></line><line># Get a list of the first 10 motes. Note that the value after the</line><line># colon denotes the COUNT, and -not- the last index.</line><line>nodes = T[0:10]</line><line>print &gt;&gt;OUT, &quot;First 10 nodes: &quot;, getIDs(nodes)</line><line></line><line># Get nodes 1, 8 and 24. Note the second pair of brackets;</line><line># these are used to specify a List (python calls arrays Lists) as</line><line># an argument to T[].</line><line>nodes = T[[1,8,24]]</line><line>print &gt;&gt;OUT, &quot;Nodes 1, 8 and 24: &quot;, getIDs(nodes)</line><line></line><line># To get a single mote, you can use T[22] this is the same</line><line># as T.getNode(22):</line><line>print &gt;&gt;OUT, &quot;Node 22 is: &quot;, T[22].id()</line></template>