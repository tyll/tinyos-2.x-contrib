README for Jostle 
Author/Contact: jhiner@ece.arizona.edu

Description:

Jostle is an acceleration testbench application. It periodically samples the
AccelX sensor if one exists.  It turns on the green led if positive acceleration
exceeds some threshold, and turns the led off if negative acceleration exceeds it.

Tools:

None.

Known bugs/limitations:

None.
